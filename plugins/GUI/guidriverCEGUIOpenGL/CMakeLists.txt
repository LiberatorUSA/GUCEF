#-------------------------------------------------------------------
# This file was automatically generated by ProjectGenerator
# which is tooling part the build system designed for GUCEF
#     (Galaxy Unlimited Framework)
# For the latest info, see http://www.VanvelzenSoftware.com/
#
# The contents of this file are placed in the public domain. Feel
# free to make use of it in any way you like.
#-------------------------------------------------------------------
#

# Configuration for module: guidriverCEGUIOpenGL

set( HEADER_FILES 
  include/guidriverCEGUIOpenGL.h
  include/guidriverCEGUIOpenGL_CGUIContextGL.h
  include/guidriverCEGUIOpenGL_CGUIDriverGL.h
  include/guidriverCEGUIOpenGL_CImageLoader.h
  include/guidriverCEGUIOpenGL_config.h
  include/guidriverCEGUIOpenGL_macros.h
  include/guidriverCEGUIOpenGL_pluginAPI.h
)

set( SOURCE_FILES 
  src/guidriverCEGUIOpenGL.cpp
  src/guidriverCEGUIOpenGL_CGUIContextGL.cpp
  src/guidriverCEGUIOpenGL_CGUIDriverGL.cpp
  src/guidriverCEGUIOpenGL_CImageLoader.cpp
  src/guidriverCEGUIOpenGL_pluginAPI.cpp
)

# Make sure the PLATFORM_HEADER_FILES variable is always defined
 set( PLATFORM_HEADER_FILES "" )

# Make sure the PLATFORM_SOURCE_FILES variable is always defined
 set( PLATFORM_SOURCE_FILES "" )

# For ease of use make a variable that has all files for this module
set( ALL_FILES ${HEADER_FILES} ${SOURCE_FILES} ${PLATFORM_HEADER_FILES} ${PLATFORM_SOURCE_FILES} )

set( MODULE_NAME "guidriverCEGUIOpenGL" )
add_library( ${MODULE_NAME} ${ALL_FILES} )
add_dependencies( ${MODULE_NAME} CEGUI gucefCORE gucefGUI gucefIMAGE gucefINPUT gucefMT gucefVFS guidriverCEGUI )
target_link_libraries( ${MODULE_NAME} CEGUI gucefCORE gucefGUI gucefIMAGE gucefINPUT gucefMT gucefVFS guidriverCEGUI )
set_property( TARGET ${MODULE_NAME} APPEND_STRING PROPERTY COMPILE_DEFINITIONS "GUIDRIVERCEGUIOPENGL_BUILD_MODULE" )

if ( ANDROID )
  add_dependencies( ${MODULE_NAME} CEGUI.RendererModule.OpenGLES )
  target_link_libraries( ${MODULE_NAME} CEGUI.RendererModule.OpenGLES GLESv1_CM )
endif( ANDROID )

if ( LINUX )
  add_dependencies( ${MODULE_NAME} CEGUI.RendererModule.OpenGL )
  target_link_libraries( ${MODULE_NAME} CEGUI.RendererModule.OpenGL GL )
endif( LINUX )

if ( WIN32 )
  add_dependencies( ${MODULE_NAME} CEGUI.RendererModule.OpenGL )
  target_link_libraries( ${MODULE_NAME} CEGUI.RendererModule.OpenGL OpenGL32 )
endif( WIN32 )

if ( WIN64 )
  add_dependencies( ${MODULE_NAME} CEGUI.RendererModule.OpenGL )
  target_link_libraries( ${MODULE_NAME} CEGUI.RendererModule.OpenGL OpenGL32 )
endif( WIN64 )

include_directories( ../../../common/include ../../../dependencies/cegui/cegui/include ../../../dependencies/freetype/include ../../../dependencies/freetype/include/freetype ../../../dependencies/freetype/include/freetype/config ../../../dependencies/freetype/include/freetype/internal ../../../dependencies/freetype/include/freetype/internal/services ../../../dependencies/freetype/src/winfonts ../../../gucefCORE/include ../../../gucefGUI/include ../../../gucefIMAGE/include ../../../gucefINPUT/include ../../../gucefMT/include ../../../gucefVFS/include ../guidriverCEGUI/include include )

if ( ANDROID )
  include_directories( ../../../gucefCORE/include/android )
endif( ANDROID )

if ( LINUX )
  include_directories( ../../../gucefCORE/include/linux )
endif( LINUX )

if ( WIN32 )
  include_directories( ../../../gucefCORE/include/mswin )
endif( WIN32 )

if ( WIN64 )
  include_directories( ../../../gucefCORE/include/mswin )
endif( WIN64 )



#******** START OF AUTOGENERATED TEMPLATED ADDITION CONTENT ********




if (ANDROID)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (LINUX)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (OSX)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (WIN32)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (WIN64)
GUCEF_config_lib( ${MODULE_NAME} )
else()
GUCEF_config_lib( ${MODULE_NAME} )
endif()



#******** END OF AUTOGENERATED TEMPLATED ADDITION CONTENT ********


