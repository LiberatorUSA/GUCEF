#-------------------------------------------------------------------
# This file was automatically generated by ProjectGenerator
# which is tooling part the build system designed for GUCEF
#     (Galaxy Unlimited Framework)
# For the latest info, see http://www.VanvelzenSoftware.com/
#
# The contents of this file are placed in the public domain. Feel
# free to make use of it in any way you like.
#-------------------------------------------------------------------
#

# Configuration for module: dstorepluginJSONPARSER

set( HEADER_FILES 
  include/DLLMainDSTOREpluginJSONPARSER.h
)

set( SOURCE_FILES 
  src/DLLMainDSTOREpluginJSONPARSER.c
)

# Make sure the PLATFORM_HEADER_FILES variable is always defined
 set( PLATFORM_HEADER_FILES "" )

# Make sure the PLATFORM_SOURCE_FILES variable is always defined
 set( PLATFORM_SOURCE_FILES "" )

# For ease of use make a variable that has all files for this module
set( ALL_FILES ${HEADER_FILES} ${SOURCE_FILES} ${PLATFORM_HEADER_FILES} ${PLATFORM_SOURCE_FILES} )

set( MODULE_NAME "dstorepluginJSONPARSER" )
add_library( ${MODULE_NAME} ${ALL_FILES} )
add_dependencies( ${MODULE_NAME} gucefCORE gucefMT jsonbuilder jsonparser )
target_link_libraries( ${MODULE_NAME} jsonbuilder jsonparser )
set_property( TARGET ${MODULE_NAME} APPEND_STRING PROPERTY COMPILE_DEFINITIONS "DSTOREPLUGINJSONPARSER_BUILD_MODULE" )

include_directories( ../../../common/include ../../../dependencies/json-builder ../../../dependencies/json-parser ../../../platform/gucefCORE/include ../../../platform/gucefMT/include include )

if ( ANDROID )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser ../../../platform/gucefCORE/include/android )
endif( ANDROID )

if ( EMSCRIPTEN )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser )
endif( EMSCRIPTEN )

if ( GLX )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser )
endif( GLX )

if ( GTK )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser )
endif( GTK )

if ( IOS )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser )
endif( IOS )

if ( LINUX32 )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser ../../../platform/gucefCORE/include/linux )
endif( LINUX32 )

if ( LINUX64 )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser ../../../platform/gucefCORE/include/linux )
endif( LINUX64 )

if ( NACL )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser )
endif( NACL )

if ( OSX )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser )
endif( OSX )

if ( POSIX )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser )
endif( POSIX )

if ( SDL )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser )
endif( SDL )

if ( SYMBIAN )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser )
endif( SYMBIAN )

if ( UNIX )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser )
endif( UNIX )

if ( WIN32 )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser ../../../platform/gucefCORE/include/mswin ../../../platform/gucefMT/include/mswin )
endif( WIN32 )

if ( WIN64 )
  include_directories( ../../../dependencies/json-builder ../../../dependencies/json-parser ../../../platform/gucefCORE/include/mswin ../../../platform/gucefMT/include/mswin )
endif( WIN64 )



#******** START OF AUTOGENERATED TEMPLATED ADDITION CONTENT ********




if (ANDROID)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (EMSCRIPTEN)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (GLX)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (GTK)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (IOS)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (LINUX32)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (LINUX64)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (NACL)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (OSX)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (POSIX)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (SDL)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (SYMBIAN)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (UNIX)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (WIN32)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (WIN64)
GUCEF_config_lib( ${MODULE_NAME} )
else()
GUCEF_config_lib( ${MODULE_NAME} )
endif()



#******** END OF AUTOGENERATED TEMPLATED ADDITION CONTENT ********


