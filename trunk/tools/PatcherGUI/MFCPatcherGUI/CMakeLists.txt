#-------------------------------------------------------------------
# This file was automatically generated by ProjectGenerator
# which is tooling part the build system designed for GUCEF
#     (Galaxy Unlimited Framework)
# For the latest info, see http://www.VanvelzenSoftware.com/
#
# The contents of this file are placed in the public domain. Feel
# free to make use of it in any way you like.
#-------------------------------------------------------------------
#

# Configuration for module: MFCPatcherGUI



if (WIN32)
  set( PLATFORM_HEADER_FILES 
    include/CGUCEFAppWin32MFCDriver.h
    include/ChildView.h
    include/CMFCCommandLineInfo.h
    include/MainFrm.h
    include/MFCPatcherGUI.h
    include/stdafx.h
    projects/MVC8/res/Resource.h
    projects/MVC9/res/Resource.h
  )

  set( PLATFORM_HEADER_INSTALL "WIN32" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    src/CGUCEFAppWin32MFCDriver.cpp
    src/ChildView.cpp
    src/CMFCCommandLineInfo.cpp
    src/MainFrm.cpp
    src/MFCPatcherGUI.cpp
    src/stdafx.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "WIN32" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (WIN64)
  set( PLATFORM_HEADER_FILES 
    include/CGUCEFAppWin32MFCDriver.h
    include/ChildView.h
    include/CMFCCommandLineInfo.h
    include/MainFrm.h
    include/MFCPatcherGUI.h
    include/stdafx.h
    projects/MVC8/res/Resource.h
    projects/MVC9/res/Resource.h
  )

  set( PLATFORM_HEADER_INSTALL "WIN64" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    src/CGUCEFAppWin32MFCDriver.cpp
    src/ChildView.cpp
    src/CMFCCommandLineInfo.cpp
    src/MainFrm.cpp
    src/MFCPatcherGUI.cpp
    src/stdafx.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "WIN64" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

endif ()

# For ease of use make a variable that has all files for this module
set( ALL_FILES ${HEADER_FILES} ${SOURCE_FILES} ${PLATFORM_HEADER_FILES} ${PLATFORM_SOURCE_FILES} )


if( WIN32 )
  set( MODULE_NAME "MFCPatcherGUI" )
elseif( WIN64 )
  set( MODULE_NAME "MFCPatcherGUI" )
endif()
if( WIN32 )
  add_library( ${MODULE_NAME} ${ALL_FILES} )
elseif( WIN64 )
  add_library( ${MODULE_NAME} ${ALL_FILES} )
endif()

if ( WIN32 )
  add_dependencies( ${MODULE_NAME} gucefPATCHER gucefCOMCORE gucefVFS gucefCOM gucefMT gucefCORE )
  target_link_libraries( ${MODULE_NAME} gucefCOM gucefMT gucefCORE gucefVFS gucefPATCHER gucefCOMCORE )
  set_target_properties( ${MODULE_NAME} PROPERTIES COMPILE_DEFINITIONS "_AFXDLL" )
endif( WIN32 )

if ( WIN64 )
  add_dependencies( ${MODULE_NAME} gucefPATCHER gucefCOMCORE gucefVFS gucefCOM gucefMT gucefCORE )
  target_link_libraries( ${MODULE_NAME} gucefCOM gucefMT gucefCORE gucefVFS gucefPATCHER gucefCOMCORE )
  set_target_properties( ${MODULE_NAME} PROPERTIES COMPILE_DEFINITIONS "_AFXDLL" )
endif( WIN64 )

include_directories( ../../../common/include ../../../gucefCOM/include ../../../gucefCOMCORE/include ../../../gucefCORE/include ../../../gucefMT/include ../../../gucefPATCHER/include ../../../gucefVFS/include )

if ( WIN32 )
  include_directories( ../../../gucefCOMCORE/include/mswin ../../../gucefCORE/include/mswin include projects/MVC8/res projects/MVC9/res )
endif( WIN32 )

if ( WIN64 )
  include_directories( ../../../gucefCOMCORE/include/mswin ../../../gucefCORE/include/mswin include projects/MVC8/res projects/MVC9/res )
endif( WIN64 )



#******** START OF AUTOGENERATED TEMPLATED ADDITION CONTENT ********




if (WIN32)
GUCEF_config_lib( ${MODULE_NAME} )
elseif (WIN64)
GUCEF_config_lib( ${MODULE_NAME} )
endif()



#******** END OF AUTOGENERATED TEMPLATED ADDITION CONTENT ********


