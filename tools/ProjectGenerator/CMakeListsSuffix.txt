
if (WIN32)
  add_executable( ProjectGenerator WIN32 ${HEADER_FILES} ${SOURCE_FILES} )
else()
  add_executable( ProjectGenerator ${HEADER_FILES} ${SOURCE_FILES} )
endif()
add_dependencies( ProjectGenerator gucefCORE gucefMT)
target_link_libraries( ProjectGenerator gucefCORE gucefMT )
set_target_properties( ProjectGenerator PROPERTIES COMPILE_DEFINITIONS PROJECTGENERATOR_BUILD_MODULE )
GUCEF_config_tool( ProjectGenerator )