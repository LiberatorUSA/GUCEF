
if (WIN32)
  add_executable( PatchSetGenerator WIN32 ${HEADER_FILES} ${SOURCE_FILES} )
else()
  add_executable( PatchSetGenerator ${HEADER_FILES} ${SOURCE_FILES} )
endif()
add_dependencies( PatchSetGenerator gucefCORE gucefMT gucefPATCHER )
target_link_libraries( PatchSetGenerator gucefCORE gucefMT gucefPATCHER )
set_target_properties( PatchSetGenerator PROPERTIES COMPILE_DEFINITIONS PATCHSETGENERATOR_BUILD_MODULE )
GUCEF_config_tool( PatchSetGenerator )
