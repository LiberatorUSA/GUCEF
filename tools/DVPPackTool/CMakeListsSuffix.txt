
if (WIN32)
  add_executable( DVPPackTool WIN32 ${HEADER_FILES} ${SOURCE_FILES} )
else()
  add_executable( DVPPackTool ${HEADER_FILES} ${SOURCE_FILES} )
endif()
add_dependencies( DVPPackTool gucefCORE gucefMT )
target_link_libraries( DVPPackTool gucefCORE gucefMT )
set_target_properties( DVPPackTool PROPERTIES COMPILE_DEFINITIONS DVPPACKTOOL_BUILD_MODULE )
include_directories( ../../gucefMT/include ../../gucefCORE/include )
GUCEF_config_tool( DVPPackTool )